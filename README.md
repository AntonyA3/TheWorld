A proof of concept forum where threads are created on a planet.
To run it requires php, it can be run with the command in a terminal with 
php -S localhost:8000
and can be accessed with
http://localhost:8000/index.html
